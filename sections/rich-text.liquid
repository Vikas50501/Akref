{%- comment -%} Reusable rich-text / heading section {%- endcomment -%}
{%- style -%}
  #shopify-section-{{ section.id }} { padding-top: {{ section.settings.padding_top }}px; padding-bottom: {{ section.settings.padding_bottom }}px; background: {{ section.settings.bg }}; color: {{ section.settings.text_color }}; text-align: {{ section.settings.alignment }}; }
{%- endstyle -%}
<div class="rich-text index-section">
  <div class="container {% if section.settings.width == 'narrow' %}container--text{% elsif section.settings.width == 'medium' %}container--narrow{% endif %}">
    <div class="rich-text__inner" data-anim="up">
      {%- for block in section.blocks -%}
        {%- case block.type -%}
          {%- when 'tagline' -%}<p class="tag-line" {{ block.shopify_attributes }}>{{ block.settings.text }}</p>
          {%- when 'heading' -%}<h2 class="rich-text__heading letters-slide-up text-split" {{ block.shopify_attributes }}>{{ block.settings.text }}</h2>
          {%- when 'text' -%}<div class="rich-text__body rte" {{ block.shopify_attributes }}>{{ block.settings.text }}</div>
          {%- when 'button' -%}<a href="{{ block.settings.link }}" class="button {{ block.settings.style }}" {{ block.shopify_attributes }}>{{ block.settings.label }}</a>
        {%- endcase -%}
      {%- endfor -%}
    </div>
  </div>
</div>
{% stylesheet %}
.rich-text__inner { display: flex; flex-direction: column; gap: 18px; align-items: inherit; }
.rich-text[style*="center"] .rich-text__inner { align-items: center; }
.rich-text__heading { font-size: clamp(26px, 3.4vw, 40px); }
.rich-text__body { max-width: 720px; }
{% endstylesheet %}
{% schema %}
{
  "name": "t:sections.rich-text.name",
  "class": "index-section rich-text-section",
  "settings": [
    {
      "type": "select",
      "id": "alignment",
      "label": "t:sections.rich-text.settings.alignment.label",
      "options": [
        {
          "value": "left",
          "label": "t:sections.rich-text.settings.alignment.options.o0"
        },
        {
          "value": "center",
          "label": "t:sections.rich-text.settings.alignment.options.o1"
        }
      ],
      "default": "center"
    },
    {
      "type": "select",
      "id": "width",
      "label": "t:sections.rich-text.settings.width.label",
      "options": [
        {
          "value": "narrow",
          "label": "t:sections.rich-text.settings.width.options.o0"
        },
        {
          "value": "medium",
          "label": "t:sections.rich-text.settings.width.options.o1"
        },
        {
          "value": "wide",
          "label": "t:sections.rich-text.settings.width.options.o2"
        }
      ],
      "default": "medium"
    },
    {
      "type": "color",
      "id": "bg",
      "label": "t:sections.rich-text.settings.bg.label",
      "default": "#ffffff"
    },
    {
      "type": "color",
      "id": "text_color",
      "label": "t:sections.rich-text.settings.text_color.label",
      "default": "#141515"
    },
    {
      "type": "range",
      "id": "padding_top",
      "label": "t:sections.rich-text.settings.padding_top.label",
      "min": 0,
      "max": 140,
      "step": 4,
      "unit": "px",
      "default": 60
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "label": "t:sections.rich-text.settings.padding_bottom.label",
      "min": 0,
      "max": 140,
      "step": 4,
      "unit": "px",
      "default": 60
    }
  ],
  "blocks": [
    {
      "type": "tagline",
      "name": "t:sections.rich-text.blocks.tagline.name",
      "settings": [
        {
          "type": "text",
          "id": "text",
          "label": "t:sections.rich-text.blocks.tagline.settings.text.label"
        }
      ]
    },
    {
      "type": "heading",
      "name": "t:sections.rich-text.blocks.heading.name",
      "settings": [
        {
          "type": "text",
          "id": "text",
          "label": "t:sections.rich-text.blocks.heading.settings.text.label",
          "default": "Heading"
        }
      ]
    },
    {
      "type": "text",
      "name": "t:sections.rich-text.blocks.text.name",
      "settings": [
        {
          "type": "richtext",
          "id": "text",
          "label": "t:sections.rich-text.blocks.text.settings.text.label",
          "default": "<p>Share information about your brand.</p>"
        }
      ]
    },
    {
      "type": "button",
      "name": "t:sections.rich-text.blocks.button.name",
      "settings": [
        {
          "type": "text",
          "id": "label",
          "label": "t:sections.rich-text.blocks.button.settings.label.label"
        },
        {
          "type": "url",
          "id": "link",
          "label": "t:sections.rich-text.blocks.button.settings.link.label"
        },
        {
          "type": "select",
          "id": "style",
          "label": "t:sections.rich-text.blocks.button.settings.style.label",
          "options": [
            {
              "value": "",
              "label": "t:sections.rich-text.blocks.button.settings.style.options.o0"
            },
            {
              "value": "alt",
              "label": "t:sections.rich-text.blocks.button.settings.style.options.o1"
            }
          ],
          "default": ""
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Rich text",
      "blocks": [
        {
          "type": "heading"
        },
        {
          "type": "text"
        }
      ]
    }
  ]
}
{% endschema %}
